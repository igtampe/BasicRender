Its sad that this is the only way to create a folder. Even if its a git limitation. Maybe Github could actually just make a button that says "Create folder" and then make two commits to just do this automatically. Create a file to create the folder, then delete the file 🤷‍♂️ 
